<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yutons.cms.dao.admin.DeptDao">
    <!--auto generated Code-->
    <resultMap id="BaseResultMap" type="com.yutons.cms.bean.admin.Dept">
        <result column="id" property="id" jdbcType="INTEGER"/>
        <result column="code" property="code" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="parantid" property="parantid" jdbcType="INTEGER"/>
    </resultMap>

    <!--auto generated Code-->
    <sql id="Base_Column_List">
        id,
        code,
        name,
        parantid
    </sql>

    <!--auto generated Code-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="dept.id">
        INSERT INTO t_dept (
        code,
        name,
        parantid
        ) VALUES (
        #{dept.code,jdbcType=VARCHAR},
        #{dept.name,jdbcType=VARCHAR},
        #{dept.parantid,jdbcType=INTEGER}
        )
    </insert>

    <!--auto generated Code-->
    <insert id="insertSelective" useGeneratedKeys="true" keyProperty="dept.id">
        INSERT INTO t_dept
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="dept.id!=null"> id,</if>
            <if test="dept.code!=null"> code,</if>
            <if test="dept.name!=null"> name,</if>
            <if test="dept.parantid!=null"> parantid,</if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="dept.id!=null">#{dept.id,jdbcType=INTEGER},
            </if>
            <if test="dept.code!=null">#{dept.code,jdbcType=VARCHAR},
            </if>
            <if test="dept.name!=null">#{dept.name,jdbcType=VARCHAR},
            </if>
            <if test="dept.parantid!=null">#{dept.parantid,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>

    <!--auto generated Code-->
    <insert id="insertList">
        INSERT INTO t_dept (
        <include refid="Base_Column_List"/>
        )VALUES
        <foreach collection="depts" item="dept" index="index" separator=",">
            (
            #{dept.id,jdbcType=INTEGER},
            #{dept.code,jdbcType=VARCHAR},
            #{dept.name,jdbcType=VARCHAR},
            #{dept.parantid,jdbcType=INTEGER}
            )
        </foreach>
    </insert>

    <!--auto generated Code-->
    <update id="update">
        UPDATE t_dept
        <set>
            <if test="dept.code != null"> code= #{dept.code,jdbcType=VARCHAR},</if>
            <if test="dept.name != null"> name= #{dept.name,jdbcType=VARCHAR},</if>
            <if test="dept.parantid != null"> parantid= #{dept.parantid,jdbcType=INTEGER}</if>
        </set>
        WHERE id = #{dept.id,jdbcType=INTEGER}
    </update>
    <select id="selectDepts" resultType="dept">
        <!---->
        SELECT
        t_dept.id,
        t_dept.`code`,
        t_dept.`name`,
        t_dept.parantid
        FROM
        t_dept
    </select>
    <select id="selectDeptPageByCondition" resultType="dept">
        <!---->
        SELECT
        (@r :=@r + 1) AS id,
        t_dept.id deptId,
        t_dept.`code`,
        t_dept.`name`,
        t_dept.parantid
        FROM
        t_dept,
        (SELECT @r := ${(dept.page-1)*dept.limit}) r
        <where>
            <if test="dept.id!=null">
                t_dept.id=#{dept.id}
            </if>
        </where>
        <if test="dept.limit!=null">
            LIMIT ${(dept.page-1)*dept.limit},
            ${dept.limit}
        </if>
    </select>
    <select id="selectDeptsByCondition" resultType="dept">
        <!---->
        SELECT
        t_dept.id id,
        t_dept.`code`,
        t_dept.`name`,
        t_dept.parantid
        FROM
        t_dept
        <where>
            <if test="dept.id!=null">
                t_dept.id=#{dept.id}
            </if>
        </where>
        <if test="dept.limit!=null">
            LIMIT ${(dept.page-1)*dept.limit},
            ${dept.limit}
        </if>
    </select>
    <select id="selectDeptCountByCondition" resultType="int">
        <!---->
        SELECT
        count(1)
        FROM
        t_dept
    </select>

<!--auto generated by codehelper on 2017-11-16 10:12:53-->
	<select id="selectDeptById" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_dept
        where id=#{dept.id,jdbcType=INTEGER}
    </select>
    <delete id="deleteDeptById">
        DELETE FROM t_dept where id=#{dept.id}
    </delete>
</mapper>

